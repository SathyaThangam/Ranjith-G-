import {StyleSheet} from 'react-native';

const CommonStyles = StyleSheet.create({
  horizontalView: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  greyBG: {
    backgroundColor: '#ccc',
  },
  basicBtn: {
    width: '100%',
    padding: 10,
    alignItems: 'center',
  },
});

export default CommonStyles;
